# name: Build and Deploy backend-service

# on:
#   push:
#     branches: [main]

# # Required for OIDC — without this, GitHub won't issue the token needed to
# # assume the AWS role. This is the piece that replaces static AWS keys.
# permissions:
#   id-token: write
#   contents: read

# env:
#   AWS_REGION: us-east-1
#   ECR_REPOSITORY: acme-cloud-poc-backend
#   EKS_CLUSTER_NAME: acme-cloud-poc-eks

# jobs:
#   build-and-deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       # No AWS access key or secret anywhere in this workflow — this is the
#       # OIDC trust chain built in Phase 5. GitHub mints a short-lived token,
#       # AWS verifies it against acme-cloud-poc-github-deploy-role's trust
#       # policy (scoped to this exact repo), and issues temporary credentials.
#       - name: Configure AWS credentials via OIDC
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::338449997393:role/acme-cloud-poc-github-deploy-role
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: ecr-login
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Build and push image
#         env:
#           ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#           echo "IMAGE_URI=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_ENV

#       # kubectl auth here works because of the EKS Access Entry created in
#       # Phase 5, mapping this same IAM role to AmazonEKSEditPolicy inside
#       # the cluster's RBAC — IAM alone wouldn't be enough without it.
#       - name: Update kubeconfig
#         run: aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

#       - name: Deploy to EKS
#         run: |
#           sed -i "s|IMAGE_PLACEHOLDER|${IMAGE_URI}|g" k8s/deployment.yaml
#           kubectl apply -f k8s/deployment.yaml
#           kubectl apply -f k8s/service.yaml
#           kubectl apply -f k8s/ingress.yaml
#           kubectl rollout status deployment/backend-service --timeout=180s
